begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

begin;
DECLARE test SCROLL CURSOR FOR SELECT a.id id1, b.id id2 FROM testao a, testao b where a.name = b.name order by b.id desc;

FETCH FORWARD 50 FROM test;

CLOSE test;

COMMIT WORK;

